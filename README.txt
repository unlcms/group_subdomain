Group Subdomain (group_subdomain) for Drupal 8

---------

Things hardcoded that could be generalized:
 - field_use_subdomain is needed and is setup through GUI
 - Regex matches groups at paths like /group/*
